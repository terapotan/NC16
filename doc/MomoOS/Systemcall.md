# MomoOSシステムコール
## MomoOSシステムコール呼出し手順
　MomoOSのシステムコールを呼び出すには、aレジスタに呼び出したいシステムコールに対応する数値（以下システムコールナンバーと呼ぶ）を入れて、syscall命令を呼び出します。以下はサンプルコードです。

```nasm
mov a,0
syscall
```

　なおsyscall命令は内部的には割り込み番号511の割り込みを呼び出しています。
## システムコールナンバー対応表
　以下にシステムコールナンバーに対応する関数を示します。

|システムコールナンバー|対応する関数の名前|
|:--:|:--:|
